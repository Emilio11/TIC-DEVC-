///////algoritmos de ordenacion
//metodo de la burbuja;si o no
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
using namespace std;
//MANEJO DE PUNTEROS  
struct persona{
       string nombre;//guarda palabraen la memoria
       int edad;//guarda la edad (numero entero) en la memoria

int main(){
    int a;//numero entero
    float peso;//numero decimal
    char nombre[10];// vector array 10 posiciones de memoria,letra por posición
    int notas[30];//vector que guarda 30 numeros enteros
    char letra
    letra="A"
    char *pa;
    pa=&letra;
    printf("%x",&letra)//alponer el ampersan te da la dirección de memoria
    printf("%c",letra)//sale el contenido de letra = A
    printf("%x", pa)//sale 11AA00, alponer %x te pone la direccion de memoria,%i ---<entero, %s----<string,%f---<decimalfloat)i
    printf("%c",*pa)//sale el contenido de letra = A
    char *ganadores[3];
    malloc(
}
